---
phase: 02-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/components/landing/HeroSection.tsx
  - src/components/landing/HowItWorksSection.tsx
  - src/components/landing/CompetitorSection.tsx
  - src/components/landing/PricingSection.tsx
  - src/components/landing/CredibilitySection.tsx
  - src/components/landing/CtaSection.tsx
  - src/components/landing/StickyApplyCta.tsx
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04
  - LAND-05
  - LAND-06
  - LAND-08
  - LAND-09

must_haves:
  truths:
    - "User lands on a dark (#0a0a0a) page with centered MVR logo and 'Secure Exclusive ZIP Access Before Public Launch' serif headline"
    - "User sees two CTA buttons in hero: 'Apply for Founding Seat' linking to /apply and 'See How It Works' scrolling to that section"
    - "User scrolls through How It Works, Competitor Contrast (naming Zillow and HAR), Pricing ($100 activation credit), Credibility, and final CTA sections in that order"
    - "Content sections fade in and slide up as they enter the viewport via motion scroll animations"
    - "Sticky floating Apply CTA appears after scrolling past hero (pill on desktop, full-width bar on mobile)"
    - "Page is fully usable on a 375px mobile viewport"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Dark theme variables, gold accent, serif font"
      contains: "--color-gold"
    - path: "src/app/layout.tsx"
      provides: "Playfair Display serif font + updated metadata"
      contains: "Playfair_Display"
    - path: "src/app/page.tsx"
      provides: "Composed landing page with all sections"
      min_lines: 20
    - path: "src/components/landing/HeroSection.tsx"
      provides: "Hero with logo, headline, subheadline, CTA buttons, parallax"
      contains: "motion"
    - path: "src/components/landing/HowItWorksSection.tsx"
      provides: "How It Works explanation with motion animation"
      contains: "whileInView"
    - path: "src/components/landing/CompetitorSection.tsx"
      provides: "Competitor contrast naming Zillow and HAR"
      contains: "Zillow"
    - path: "src/components/landing/PricingSection.tsx"
      provides: "Price anchor with $100 activation credit"
      contains: "$100"
    - path: "src/components/landing/CredibilitySection.tsx"
      provides: "Founder credibility section"
      contains: "whileInView"
    - path: "src/components/landing/CtaSection.tsx"
      provides: "Final CTA section ending the page"
      contains: "/apply"
    - path: "src/components/landing/StickyApplyCta.tsx"
      provides: "Sticky floating CTA (desktop pill + mobile bar)"
      contains: "IntersectionObserver"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/landing/HeroSection.tsx"
      via: "import and render"
      pattern: "import.*HeroSection"
    - from: "src/components/landing/HeroSection.tsx"
      to: "/apply"
      via: "CTA button href"
      pattern: "href.*\\/apply"
    - from: "src/components/landing/StickyApplyCta.tsx"
      to: "hero section"
      via: "IntersectionObserver on #hero element"
      pattern: "getElementById.*hero"
---

<objective>
Build the complete dark-themed landing page with all static content sections, scroll animations, and sticky CTA navigation.

Purpose: Create the full conversion surface that a Houston real estate professional sees when they land on the site -- dark minimal design with premium serif typography, gold accent highlights, scroll-triggered animations, aggressive competitor contrast copy, pricing anchor, and clear path to apply.

Output: A complete landing page at `/` with 7 section components (Hero, How It Works, Competitor Contrast, Pricing, Credibility, Final CTA) + a sticky floating Apply CTA, all composed in `page.tsx`. The only missing piece is the live seat checker (Plan 02).
</objective>

<execution_context>
@/Users/benjaminbarwo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benjaminbarwo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-landing-page/02-CONTEXT.md
@.planning/phases/02-landing-page/02-RESEARCH.md
@src/app/globals.css
@src/app/layout.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and configure dark theme + serif typography</name>
  <files>
    package.json
    src/app/globals.css
    src/app/layout.tsx
  </files>
  <action>
    1. Install new dependencies:
       ```bash
       npm install motion use-debounce
       ```
       `motion` is the correct package (NOT `framer-motion`) for React 19 support. `use-debounce` is for the seat checker in Plan 02 but install now to avoid dependency issues later.

    2. Update `src/app/globals.css`:
       - Remove the `:root` light theme variables block entirely
       - Remove the `@media (prefers-color-scheme: dark)` block entirely (this page is always dark -- no light mode)
       - Update `@theme inline` to include:
         - `--font-serif: var(--font-playfair);` (Playfair Display variable from layout.tsx)
         - `--color-background: #0a0a0a;` (locked dark background)
         - `--color-foreground: #ededed;`
         - `--color-gold: #c9a84c;` (gold accent -- CTAs, price anchor, seat numbers)
         - `--color-gold-muted: #a8893c;` (hover state for gold elements)
         - `--color-surface: #111111;` (slightly lighter dark for cards/sections)
         - `--color-muted: #6b7280;` (gray for secondary copy)
       - Keep `--font-sans` and `--font-mono` existing variables
       - Update `body` rule: `background-color: var(--color-background); color: var(--color-foreground); font-family: var(--font-sans), Arial, Helvetica, sans-serif;`

    3. Update `src/app/layout.tsx`:
       - Import `Playfair_Display` from `next/font/google` alongside existing Geist imports
       - Initialize: `const playfair = Playfair_Display({ variable: "--font-playfair", subsets: ["latin"], display: "swap" });`
       - Add `playfair.variable` to the `<html>` className chain: `${geistSans.variable} ${geistMono.variable} ${playfair.variable}`
       - Update metadata: `title: "MVR Founding Seats | Secure Exclusive ZIP Access"`, `description: "Claim your founding seat in Houston's first ZIP-level real estate routing network. Limited seats per ZIP and role. $100 activation credit applied to your first live month."`
       - Add to body className: `antialiased bg-background text-foreground`
  </action>
  <verify>
    - `npm run build` completes without errors (validates fonts load, Tailwind compiles, no import issues)
    - Check `globals.css` has no `@media (prefers-color-scheme: dark)` block
    - Check `layout.tsx` has `Playfair_Display` import and variable in className
  </verify>
  <done>
    - Dark theme is hardcoded (no light mode toggle, no media query)
    - Gold accent color available as `text-gold`, `bg-gold`, `border-gold` in Tailwind
    - Serif font available as `font-serif` in Tailwind
    - `npm run build` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Build all static section components, sticky CTA, and compose landing page</name>
  <files>
    src/components/landing/HeroSection.tsx
    src/components/landing/HowItWorksSection.tsx
    src/components/landing/CompetitorSection.tsx
    src/components/landing/PricingSection.tsx
    src/components/landing/CredibilitySection.tsx
    src/components/landing/CtaSection.tsx
    src/components/landing/StickyApplyCta.tsx
    src/app/page.tsx
  </files>
  <action>
    All section components use `"use client"` directive for motion animations. Import `{ motion }` from `"motion/react"` (NOT `"framer-motion"`).

    **Section order (locked decision):** Hero → How It Works → Competitor Contrast → [Seat Checker placeholder] → Pricing → Credibility → CTA. No footer. Page ends on the CTA section.

    **HeroSection.tsx:**
    - `id="hero"` on the outer element (StickyApplyCta observes this)
    - Full viewport height (`min-h-screen`)
    - Parallax background layer using `useScroll` + `useTransform` from motion — subtle radial gold gradient glow that moves at 30% scroll speed
    - Centered content (vertically + horizontally):
      - MVR logo: Use `<Image src="/mvr-logo.svg" />` with `next/image` (user will place logo in `public/`). If logo not yet available, use a text placeholder "MVR" in large serif gold text.
      - Headline: "Secure Exclusive ZIP Access Before Public Launch" in `font-serif text-5xl md:text-7xl` with white color
      - Subheadline: "Get prioritized routing within your protected territory. Limited founding seats available per ZIP code and role." in `text-lg md:text-xl text-muted`
      - Two CTA buttons side by side (flex row on desktop, stack on mobile):
        - "Apply for Founding Seat" — gold background, dark text, links to `/apply`, `rounded-full px-8 py-4`
        - "See How It Works" — outlined (border-gold, text-gold), scrolls to `#how-it-works` section via `onClick` with `document.getElementById('how-it-works')?.scrollIntoView({ behavior: 'smooth' })`
    - Spacing: `py-0 px-6` (full bleed hero, generous side padding)

    **HowItWorksSection.tsx:**
    - `id="how-it-works"` for scroll target
    - `motion.section` with `whileInView` fade-in/slide-up (`initial={{ opacity: 0, y: 40 }}`, `whileInView={{ opacity: 1, y: 0 }}`, `viewport={{ once: true, amount: 0.2 }}`)
    - Section title: "How Founding Seats Work" in serif gold
    - 3-step explanation as numbered cards on dark surface background:
      1. "Choose Your Territory" — Select the ZIP codes where you want exclusive routing priority
      2. "Secure Your Seat" — Submit your $100 activation credit to lock in your founding position
      3. "Get Prioritized" — When MVR launches, founding seat holders get first access to leads in their ZIPs
    - Each card: `bg-surface rounded-2xl p-8`, number in gold, title in white, description in muted
    - Desktop: 3-column grid. Mobile: single column stack
    - Spacing: `py-32 px-6`

    **CompetitorSection.tsx:**
    - `motion.section` with whileInView animation
    - Section title: "Why Founding Seats Exist" in serif
    - Two competitor contrast blocks (naming Zillow and HAR explicitly):
      - **Zillow block:** "Zillow charges agents $1,000+/month for shared leads in oversaturated ZIP codes. You pay premium prices for prospects who are simultaneously sent to 3-4 other agents. Your close rate drops, but Zillow's revenue doesn't."
      - **HAR block:** "HAR gives every agent equal visibility regardless of investment or commitment. No priority. No territory. No competitive advantage. Just another name in a crowded list."
      - **MVR contrast:** "MVR is different. Limited founding seats per ZIP code mean less competition, not more. Your territory. Your leads. Your advantage." — gold accent on key phrases
    - Desktop: side-by-side cards for Zillow/HAR, MVR contrast below as full-width. Mobile: stacked.
    - Spacing: `py-32 px-6`

    **PricingSection.tsx:**
    - `motion.section` with whileInView animation
    - Centered layout
    - Large price display: "$100" in `text-6xl md:text-8xl font-serif text-gold font-bold`
    - Label: "Activation Credit" below the price
    - Framing line: "Applied to your first live month. Limited by ZIP and role." (per REQUIREMENTS — NO "non-refundable" language)
    - Supporting text: "Your activation credit reserves your founding seat and is applied as a credit toward your first month of live routing. Seats are limited per ZIP code and professional role."
    - Spacing: `py-32 px-6`

    **CredibilitySection.tsx:**
    - `motion.section` with whileInView animation
    - Founder/industry insider angle — operator-to-operator tone:
      - Title: "Built by Industry Insiders" in serif
      - Copy: "MVR was created by real estate professionals who were tired of paying inflated prices for shared leads. We built the routing network we wished existed — one that rewards commitment with exclusivity, not the other way around."
      - Emphasize: "This isn't another lead marketplace. It's a territory-based routing network designed by operators, for operators."
    - Spacing: `py-32 px-6`

    **CtaSection.tsx:**
    - `motion.section` with whileInView animation
    - Final persuasion before apply:
      - Title: "Ready to Claim Your Territory?" in serif, large
      - Subtitle: "Founding seats are limited. Once they're filled, they're gone."
      - Single "Apply for Founding Seat" CTA button — gold, large, links to `/apply`
    - This is the last section on the page — no footer per locked decision
    - Spacing: `py-32 px-6 pb-48` (extra bottom padding since no footer)

    **StickyApplyCta.tsx:**
    - `"use client"` — uses IntersectionObserver (NOT scroll event listener)
    - Observes `document.getElementById("hero")`:
      - When hero is NOT intersecting (scrolled past) → show sticky CTA
      - When hero IS intersecting → hide sticky CTA
    - Desktop (`hidden md:block`): Fixed pill button bottom-right (`fixed bottom-8 right-8 z-50`), gold background, shadow
    - Mobile (`md:hidden`): Full-width fixed bottom bar (`fixed bottom-0 left-0 right-0 z-50`), `bg-background/95 backdrop-blur-sm border-t border-gold/20`, padding, gold CTA button
    - Both link to `/apply`
    - Animate in/out with motion `AnimatePresence` and `motion.div` with opacity + y translation

    **page.tsx (complete replacement):**
    - Remove ALL existing Next.js starter content
    - Import and render sections in order:
      ```tsx
      import { HeroSection } from "@/components/landing/HeroSection";
      import { HowItWorksSection } from "@/components/landing/HowItWorksSection";
      import { CompetitorSection } from "@/components/landing/CompetitorSection";
      // SeatCheckerSection will be added in Plan 02
      import { PricingSection } from "@/components/landing/PricingSection";
      import { CredibilitySection } from "@/components/landing/CredibilitySection";
      import { CtaSection } from "@/components/landing/CtaSection";
      import { StickyApplyCta } from "@/components/landing/StickyApplyCta";

      export default function Home() {
        return (
          <main>
            <HeroSection />
            <HowItWorksSection />
            <CompetitorSection />
            {/* SeatCheckerSection — added in Plan 02 */}
            <PricingSection />
            <CredibilitySection />
            <CtaSection />
            <StickyApplyCta />
          </main>
        );
      }
      ```
    - page.tsx itself is a Server Component (no "use client") — it just composes client components
    - Add a comment where SeatCheckerSection will be inserted by Plan 02

    **Responsive requirements (LAND-09):**
    - All sections must be usable at 375px viewport width
    - Hero: CTA buttons stack vertically on mobile (`flex-col sm:flex-row`)
    - How It Works: 3-col grid becomes single column on mobile
    - Competitor: side-by-side cards stack on mobile
    - Text sizes: use responsive Tailwind prefixes (`text-3xl md:text-5xl` etc.)
    - Sticky CTA: pill on desktop, full-width bar on mobile

    **Animation consistency:**
    - All sections: `initial={{ opacity: 0, y: 40 }}`, `whileInView={{ opacity: 1, y: 0 }}`, `viewport={{ once: true, amount: 0.2 }}`, `transition={{ duration: 0.6, ease: "easeOut" }}`
    - Stagger child elements where applicable using `transition={{ delay: 0.1 * index }}`
    - Hero parallax uses `useScroll` + `useTransform` — background moves at 30% speed
  </action>
  <verify>
    - `npm run build` completes without errors
    - `npm run typecheck` passes
    - `npm run lint` passes (or only pre-existing warnings)
    - Visually confirm: `npm run dev` → open `http://localhost:3000` → dark page renders with all 6 sections visible on scroll
  </verify>
  <done>
    - Hero displays centered MVR text/logo, serif headline, subheadline, two CTA buttons
    - All 6 sections render in correct order: Hero → How It Works → Competitor → Pricing → Credibility → CTA
    - Sections animate in (fade + slide up) on scroll
    - Sticky Apply CTA appears after scrolling past hero
    - Page is usable at 375px mobile width — buttons stack, grid collapses, sticky bar shows on mobile
    - "Apply for Founding Seat" buttons link to `/apply`
    - "See How It Works" scrolls smoothly to the How It Works section
    - No "non-refundable" language anywhere on the page
    - Competitor section names both Zillow and HAR
    - Price shows "$100" with "Applied to your first live month" framing
  </done>
</task>

</tasks>

<verification>
1. `npm run build` — full production build passes
2. `npm run typecheck` — no TypeScript errors
3. `npm run dev` → visit `http://localhost:3000`:
   - Dark background (#0a0a0a) renders
   - Serif headline visible in hero
   - Gold accent color on CTAs and price
   - All 6 sections visible when scrolling
   - Sections animate in on scroll
   - Sticky CTA appears when hero scrolls out of view
   - Mobile: resize to 375px — layout remains usable, buttons stack, sticky bar at bottom
4. No import from `"framer-motion"` — all motion imports use `"motion/react"`
5. No `@media (prefers-color-scheme: dark)` in globals.css
</verification>

<success_criteria>
- Dark-themed landing page renders at `/` with all 6 content sections + sticky CTA
- Scroll animations work (fade in + slide up)
- Responsive at 375px mobile viewport
- Gold accent on CTAs, price, and key highlights
- Competitor section explicitly names Zillow and HAR
- Price anchor shows $100 with "Applied to your first live month" — no "non-refundable"
- Build passes, TypeScript checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page/02-01-SUMMARY.md`
</output>
